# picon 에셋

| 파일 | 쓰이는 곳 | 필드 |
|---|---|---|
| `thumbnail.jpg` | 홈·Work 목록 **카드 (클릭 전)** | `thumbnail` |
| `01.png` ~ `19.png` | **상세 페이지 본문**, 100px 간격으로 세로 배치 | `covers` |

`07.png`은 원본에 없다. 번호는 01~06, 08~19로 이어진다.

장마다 세로 비율이 다르지만(16:9 / 4:3) **좌우 폭만 컨테이너에 맞추고**
세로는 원본 비율을 지킨다. 잘리는 부분이 없다.

순서를 바꾸거나 장을 추가하려면 `src/data/projects.ts` 의 `covers` 배열을 고친다.
크기(width/height)를 함께 적어야 이미지가 로드되기 전에 자리를 잡는다.
